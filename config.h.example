#ifndef CONFIG_H
#define CONFIG_H

// ============================================================
// CONFIGURACIÓN DEL DISPOSITIVO (plantilla)
// ============================================================
// 1. Copia este archivo a `config.h` (misma carpeta).
// 2. Rellena los valores reales.
// 3. `config.h` está en .gitignore: NUNCA se sube al repo.
// ============================================================

// --- Red WiFi ---
#define WIFI_SSID "TU_RED_WIFI"
#define WIFI_PASS "TU_PASSWORD_WIFI"

// --- Bridge Server (VM Debian) ---
// STT, TTS y LLM se resuelven en el Bridge; el dispositivo no necesita
// API keys (tarea H2 completada).
#define BRIDGE_BASE_URL "http://192.168.1.XX:5000"

#endif
